<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Erreur - PowerShell Web Editor</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body>
    <div class="container mt-5">
        <div class="row">
            <div class="col-md-8 offset-md-2">
                <div class="card">
                    <div class="card-header bg-danger text-white">
                        <h4 class="mb-0">Erreur</h4>
                    </div>
                    <div class="card-body">
                        <h5 class="card-title"><%= message %></h5>
                        <% if (error) { %>
                            <div class="alert alert-danger">
                                <pre class="mb-0"><%= error.stack || error.message || error %></pre>
                            </div>
                        <% } %>
                        <div class="mt-4">
                            <a href="/auth/login" class="btn btn-primary">Réessayer la connexion</a>
                            <a href="/" class="btn btn-secondary ms-2">Retour à l'accueil</a>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
</body>
</html> 