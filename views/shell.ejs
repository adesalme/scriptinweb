<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %> - Web Editor</title>
    
    <!-- Bootstrap CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.10.0/font/bootstrap-icons.css">
    
    <!-- Bootstrap JS -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
    
    <!-- Socket.IO -->
    <script src="/socket.io/socket.io.js"></script>
    
    <%- contentFor('stylesheets') %>
    <style>
        .powershell-window {
            background-color: #002456;
            border-radius: 4px;
            overflow: hidden;
            box-shadow: 0 0 10px rgba(0,0,0,0.2);
            display: flex;
            flex-direction: column;
            height: 600px;
            margin: 20px 0;
        }

        .title-bar {
            background-color: #002456;
            color: white;
            padding: 5px 10px;
            font-size: 14px;
            border-bottom: 1px solid #003380;
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
        }

        .shell-container {
            flex: 1;
            display: flex;
            flex-direction: column;
            overflow: hidden;
            background-color: #002456;
        }

        #shell-output {
            background-color: #002456;
            color: #ffffff;
            padding: 0 10px;
            flex: 1;
            overflow-y: auto;
            white-space: pre-wrap;
            margin: 0;
            cursor: text;
            user-select: text;
            font-family: 'Consolas', monospace;
            font-size: 14px;
            line-height: 1.2;
        }

        .command-line {
            display: flex;
            background-color: #002456;
            padding: 2px 10px;
            font-family: 'Consolas', monospace;
        }

        .prompt {
            color: #ffffff;
            margin-right: 10px;
            white-space: nowrap;
        }

        #commandInput {
            font-family: 'Consolas', monospace;
            background-color: transparent;
            color: #ffffff;
            border: none;
            padding: 0;
            margin: 0;
            width: 100%;
            outline: none;
            font-size: 14px;
        }

        .success {
            color: #ffffff;
        }

        .error {
            color: #ff0000;
        }

        /* Masquer la barre de défilement tout en gardant la fonctionnalité */
        #shell-output::-webkit-scrollbar {
            width: 12px;
        }

        #shell-output::-webkit-scrollbar-track {
            background: #002456;
        }

        #shell-output::-webkit-scrollbar-thumb {
            background-color: #003380;
            border: 3px solid #002456;
            border-radius: 6px;
        }

        /* Style pour le focus */
        #shell-output:focus, #commandInput:focus {
            outline: none;
        }
    </style>
</head>
<body>
    <%- include('partials/header') %>

    <%- contentFor('body') %>
    <div class="container-fluid mt-3">
        <div class="row">
            <div class="col-12">
                <h2>Shell PowerShell</h2>
                <p class="text-muted">Exécutez des commandes PowerShell directement depuis votre navigateur.</p>

                <div class="powershell-window">
                    <div class="title-bar">
                        PowerShell
                    </div>
                    <div class="shell-container">
                        <div id="shell-output">
                            <!-- La sortie de la console sera affichée ici -->
                        </div>
                        <div class="command-line">
                            <span class="prompt">PS></span>
                            <input type="text" id="commandInput" placeholder="Entrez une commande PowerShell...">
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>

    <%- contentFor('script') %>
    <script src="/js/shell.js"></script>

    <%- include('partials/footer') %>
</body>
</html> 